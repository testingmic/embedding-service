# Transcription only - much lighter!
psutil>=5.9.0

# Choose ONE transcription library:
faster-whisper>=1.0.0  # Recommended - lighter and faster
# OR
# openai-whisper>=20230314  # Alternative (heavier, needs full PyTorch)

# Note: faster-whisper uses ONNX Runtime instead of full PyTorch
# This saves ~300-400 MB of memory!